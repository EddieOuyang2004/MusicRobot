# V2 retest — 2026-09-15

Tested the current working-tree code without modifying playback. All 25 bridge
tests and 16 matcher tests passed.

## Chronos, 60 seconds

Headless realtime execution, causal file input, 120 Hz, default Hermite backend
and shortlist, initial-motion seed 42. Speaker playback was off.

- First bridge: WA0 → `gPO_sBM_cAll_d12_mPO3_ch02`, started at audio time
  13.144 s, completed at 13.496 s.
- Next preparation entered `preparing` at 14.872 s and `failed` at 15.064 s.
  These are sampled trace timestamps, not exact solver execution times.
- Failure: `Candidates and replay infeasible: Boundary state exceeds joint limits`.
  This message reports the final rejection; it does not identify a specific joint
  or establish that every rejected candidate had the same cause.
- Terminal hold began at 18.360 s and persisted through the last sample at
  59.960 s. There was no recovery and only one completed transition.
- Thus this run failed continuous playback because no feasible plan was found,
  even though the search finished before the source endpoint. It did not reproduce
  the earlier long preparation deadline miss.

## Saved GUI result

The existing `tmp/v2_chronos_gui.csv` contains one completed transition followed
by terminal hold at 18.776 s due to unfinished preparation. Its last sample is
22.328 s. The timing report records 12 misses in 1869 iterations and a maximum
output interval of 1114.46 ms. This is a separate older run, not the new retest.

## Reproduce

```powershell
.venv/Scripts/python.exe realtime/humanoid_robot/src/realtime_music_humanoid_matcher_v2.py --headless --realtime --audio-input realtime/humanoid_robot/data/test_audio/chronos.mp3 --experiment-causal-file-input --experiment-pose-npz tmp/v2_chronos_retest_20260915_poses.npz --initial-motion-seed 42 --max-seconds 60 --trace-csv tmp/v2_chronos_retest_20260915.csv --timing-report tmp/v2_chronos_retest_20260915.json
```

Log, trace, timing report and pose recording use the
`tmp/v2_chronos_retest_20260915` prefix. This test does not validate physical
balance or collision clearance. No GUI was opened in the new run.
