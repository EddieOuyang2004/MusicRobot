# V2 retest — 2026-09-16

Current working-tree code, Chronos, 60 seconds, headless realtime causal file
input, initial-motion seed 42, default Hermite settings. Playback code was not
changed for this test. The current default control rate is **60 Hz**, unlike the
120 Hz reports from earlier tests; timing comparisons must account for this.

## Results

- 38 bridge tests and 24 matcher tests passed (62 total).
- All nine started transitions completed; zero terminal holds, replay fallbacks
  or recorded preparation failures. The final trace sample at 59.960 s was ready
  for the next transition. The current trace verifier passed.
- Every executed bridge lasted 0.35 s. Recorded preparation for those bridges
  ranged from 0.537 to 0.692 s, with 1–3 bridge attempts.
- The selected exits trimmed 0.20–1.97 authored seconds from source clips.
  This run therefore exercises the new selected-exit behavior, rather than the
  previous requirement to finish every source clip.
- No recorded executing-bridge samples were flagged as modified by output handling.
- Control: 3658 iterations, 2 deadline misses, 3 skipped periods; work p95
  3.226 ms, p99 4.726 ms, max 10.277 ms. At 60 Hz the budget is 16.667 ms.
  Maximum output interval was 55.964 ms, showing occasional scheduling gaps.
- There were 378 joint/sample position-limit exceedances before final player
  clipping, but zero final emitted joint-limit violations. This still warrants
  attention outside the accepted bridges; clipping can hide raw target excesses.

This run did not reproduce the prior freeze. It is one headless simulation run,
not validation of all songs, GUI timing, physical balance or collision clearance.
Speaker playback was off.

## Reproduce

```powershell
.venv/Scripts/python.exe realtime/humanoid_robot/src/realtime_music_humanoid_matcher_v2.py --headless --realtime --audio-input realtime/humanoid_robot/data/test_audio/chronos.mp3 --experiment-causal-file-input --experiment-pose-npz tmp/v2_chronos_retest_20260916_poses.npz --initial-motion-seed 42 --max-seconds 60 --trace-csv tmp/v2_chronos_retest_20260916.csv --timing-report tmp/v2_chronos_retest_20260916.json
```

Artifacts: trace, log, timing JSON and pose NPZ under the
`tmp/v2_chronos_retest_20260916` prefix. Specify `--control-rate-hz 60` explicitly
if reproducing after a later change to defaults.
